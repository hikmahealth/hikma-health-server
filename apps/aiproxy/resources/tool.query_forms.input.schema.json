{
  "type": "object",
  "properties": {
    "fromDate": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "string",
          "format": "date-time"
        }
      ],
      "description": "represent the start date, used in filtering the records. date is inclusive and uses RFC3339"
    },
    "toDate": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "string",
          "format": "date-time"
        }
      ],
      "description": "represent the end date, used in filtering the records. date is inclusive and uses RFC3339"
    }
  }
}