language: typescript
dependsOn:
  - js-utils

tasks:
  build:
    args: "run build"
    command: pnpm
    outputs:
      - lib/**/*
    env:
      NODE_OPTIONS: "--max-old-space-size=5120"
    deps:
      - js-utils:build
