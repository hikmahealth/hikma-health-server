language: typescript
dependsOn:
  - js-utils

tasks:
  build:
    args: "run res:build"
    command: pnpm
    outputs:
      - lib/**/*
    env:
      NODE_OPTIONS: "--max-old-space-size=5120"
    deps:
      - js-utils:build
  start:
    inputs:
      - lib/**/*
    command: npx tsx api/index.ts
