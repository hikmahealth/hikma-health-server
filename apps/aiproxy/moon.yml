language: typescript

tasks:
  build:
    args: "run res:build"
    command: pnpm
    outputs:
      - lib/**/*
    env:
      NODE_OPTIONS: "--max-old-space-size=6168"
  start:
    inputs:
      - lib/**/*
    command: npx tsx api/index.ts
