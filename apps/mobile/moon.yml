layer: application
language: typescript
stack: frontend

dependsOn:
  - id: typescript-config
    scope: peer
  - id: eslint-config
    scope: peer
  - ui
  - common
  - js-utils

# inferring tasks from scripts in the package.json
tasks: {}
