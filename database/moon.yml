tasks:
  migrate:
    # should prepare the database state before running
    # the service
    command: pnpm migrate:prod
    options:
      cache: false
  build:
    command: pnpm build
    outputs:
      - dist/**/*
  # dev:
  #   command: pnpm create_schema
  #   outputs:
  #     - /apps/web/src/db/types.d.ts
